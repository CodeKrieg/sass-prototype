<div class="navbar navbar-default navbar-static-top">
  <div class="container">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <%= link_to "ChatApp", root_path, class: "navbar-brand" %>
    <div class="navbar-collapse collapse navbar-responsive-collapse">
      <ul class="nav navbar-nav">
        <% if current_user %>
          <li><%= link_to "My Profile", my_profile_path %></li>
          <li><%= link_to "About", company_path %></li>
        <% else %>
          <li><%= link_to "Home", root_path  %></li>
          <li><%= link_to "About", company_path  %></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
          <li class="ck-badge">
             <span class="badge">
               <i class="fa fa-user fa-2x"></i>
                <%= link_to  current_user.full_name.present? ? current_user.full_name : current_user.email, my_profile_path %>
             </span>
          </li>
          <%= link_to "logout", destroy_user_session_path, method: :delete, :class => "btn btn-danger btn-md ck-button" %>
           
        <% else %>
          <%= link_to "Sign in", new_user_session_path, :class => "btn btn-success btn-md ck-button" %> 
          <%= link_to "Sign up", new_user_registration_path, :class => "btn btn-primary btn-md ck-button" %>
        <% end %>
      </ul>
    </div>
  </div>
</div>


